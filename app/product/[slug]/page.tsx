import Link from 'next/link'
import {supabaseBrowser} from '../../../lib/supabase'
export default async function Product({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=supabaseBrowser();const {data:p}=await s.from('products').select('*').eq('slug',slug).single();if(!p)return <main className="section"><div className="wrap">Produk tidak ditemukan.</div></main>;return <main className="section"><div className="wrap"><Link href="/">← Produk</Link><div className="panel" style={{marginTop:20,display:'grid',gridTemplateColumns:'1fr 1fr',gap:30}}><div className="pic">{p.image_url?<img src={p.image_url} alt={p.name} style={{width:'100%',height:'100%',objectFit:'cover'}}/>:<div className="shirt"/>}</div><div><small>{p.category}</small><h1>{p.name}</h1><h2>Rp {p.price.toLocaleString('id-ID')}</h2><p>{p.description}</p><p>Stok: {p.stock}</p><h3>Panduan ukuran</h3><p>S: 48×68 cm · M: 51×70 cm · L: 54×72 cm · XL: 57×74 cm · XXL: 60×76 cm</p><Link className="btn" href="/cart">KE KERANJANG</Link></div></div></div></main>}